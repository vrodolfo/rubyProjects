<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Show Dojo</title>
</head>
<body>
 
		<h1>Student</h1>
		<table>
			<tr>
				<td>First Name:</td>
				<td><%= @student.first_name %></td>
			</tr>
			<tr>
				<td>Last Name:</td>
				<td><%= @student.last_name %></td>
			</tr>
			<tr>
				<td>Email:</td>
				<td><%= @student.email %></td>
			</tr>
			<tr>
				<td>Dojo:</td>
				<td><%= @student.dojo.branch %></td>
			</tr>
		</table>

		<h2>Cohort</h2>
		<table>
			<tr>
				<td>First Name:</td>
				<td>Last Name:</td>
				<td>Email:</td>
			</tr>
			<% @students.each do |x| %>
				<tr>
					<td><%= x.first_name %></td>
					<td><%= x.last_name %></td>
					<td><%= x.email %></td>
				</tr>
			<% end %>
		</table>




	<a href="/goback">Home</a>
	<a href="/dojos/<%= @student.dojo.id %>/students/<%= @student.id %>/edit">Edit</a>
</body>
</html>
