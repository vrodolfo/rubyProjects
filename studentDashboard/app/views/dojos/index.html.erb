<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Dojos</title>
</head>
<body>
	<h1>Listing <%= @total %> Dojos</h1>
	<table>
		<tr>
			<td>Coding Dojo Branch</td>
			<td>Street</td>
			<td>City</td>
			<td>State</td>
			<td>Action</td>
		</tr>
		<% @dojo %>
		<% @dojo.each do |x| %>
			<tr>
				<td><%= x.branch %></td>
				<td><%= x.street %></td>
				<td><%= x.city %></td>
				<td><%= x.state %></td>
				<td>
					<a href="dojos/<%= x.id %>">Show</a> |
					<a href="dojos/<%= x.id %>/edit/">Edit</a> |
					<a href="dojos/<%= x.id %>" data-method="DELETE">Destroy</a> 
				</td>
				
			</tr>
		<% end %>
	</table>
	<a href="dojos/new">New Dojo</a>
</body>
</html>
